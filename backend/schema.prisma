// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource sqlite {
  url      = env("DATABASE_URL")
  provider = "sqlite"
}

generator client {
  provider = "prisma-client-js"
  output   = "node_modules/.prisma/client"
}

model User {
  id             String @id @default(cuid())
  name           String @default("")
  email          String @unique @default("")
  password       String
  cvs            Cv[]   @relation("User_cvs")
  from_Cv_author Cv[]   @relation("Cv_author")
}

model Cv {
  id                 String       @id @default(cuid())
  title              String       @default("")
  introduction       String       @default("")
  experiences        Experience[] @relation("Cv_experiences")
  author             User?        @relation("Cv_author", fields: [authorId], references: [id])
  authorId           String?      @map("author")
  from_User_cvs      User[]       @relation("User_cvs")
  from_Experience_cv Experience[] @relation("Experience_cv")

  @@index([authorId])
}

model Experience {
  id                  String    @id @default(cuid())
  from                DateTime? @default("2020-09-17T18:00:00.0000Z")
  to                  DateTime? @default("2020-09-17T18:00:00.0000Z")
  description         String    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  cv                  Cv?       @relation("Experience_cv", fields: [cvId], references: [id])
  cvId                String?   @map("cv")
  from_Cv_experiences Cv[]      @relation("Cv_experiences")

  @@index([cvId])
}